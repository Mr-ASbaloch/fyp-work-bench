import {StyleSheet} from 'react-native';
import { colors } from '../utils/styles';

export const privateStyle = StyleSheet.create({
    heading: {
        color: colors.common,
        fontSize: 24,
        fontWeight: '600',
        textAlign: 'center',
        marginTop: 5,
        marginBottom: 10,
      },
});
